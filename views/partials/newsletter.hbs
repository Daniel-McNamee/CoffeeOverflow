<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-12 col-md-8 col-lg-6 text-center">

            <!-- HEADING & DESCRIPTION -->
            <h2 class="fw-bold mb-3">Stay Awake with Updates ☕</h2>
            <p class="text mb-4">No spam - just beans, builds, and brew updates.</p>

            <form id="newsletterForm" class="d-flex flex-column gap-3">
                <input 
                    type="email" 
                    class="form-control p-3"
                    id="newsletterEmail" 
                    placeholder="Enter your email..."
                    required
                >

                <!-- CTA -->
                <button class="btn btn-primary py-2">Subscribe</button>

                <!-- CONFIRMATION MESSAGE -->
                <div id="newsletterMessage" class="text-success fw-semibold d-none">
                    Thanks for subscribing!
                </div>
            </form>

        </div>
    </div>
</div>
